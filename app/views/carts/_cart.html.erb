<h2>Meu Carrinho</h2>

<br/>

<table class="table table-bordered table-hover">
	<tr class="active">
		<th>Quantidade</th>
		<th>Titulo</th>
		<th>Preço</th>
		<th >Adicionar</th>
		<th>Remover</th>
		<th >Remove todos</th>
	</tr>

	<%= render cart.line_items %>

	<tr class="info">
		<td colspan="2"><b>Total</b></td>
		<td colspan="1"><b>R<%= number_to_currency(cart.total_price) %></b></td>
	</tr>
</table>

<%= button_to 'Esvaziar carrinho', cart, method: :delete, data: { confirm: 'Tem certeza?' }, class: "btn btn-danger btn-xs" %> 