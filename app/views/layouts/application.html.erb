<!DOCTYPE html>
<html>
<head>
<title>Book Store: Online Store</title>
<%= stylesheet_link_tag "application", "data-turbolinks-track" => true %>
<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
<%= csrf_meta_tags %>
</head>
<body class="<%= controller.controller_name %>">
	<div class="clearfix">
		<div class="header">
			<%=link_to image_tag('bs.png') , store_path %> 
			<%if @cart %>
			<div class="cart">
				<ul class="nav nav-pills pull-right">
					<li><a href="/carts/<%= @cart.id %>"><i class="glyphicon glyphicon-shopping-cart"></i> Carrinho <span id="cart-count">(<%= @cart.total_quantity %> - R<%= number_to_currency(@cart.total_price) %>)</span></a></li>
			</ul>
			<% end %>
		</div>

		<div class="conteudo" > 
			<%= yield %>
		</div>

		<div class="footer">
			<p> Criado por Décio e Thiago - TADS - 3M</p>
		</div>
	
	</div>
</body>
</html>
